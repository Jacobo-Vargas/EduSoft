<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.0.xsd">

    <changeSet id="create-user-courses-table" author="proyecto-ingsoft">
        <createTable tableName="user_courses">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="CURRENT_TIMESTAMP"/>
            <column name="updated_at" type="TIMESTAMP WITH TIME ZONE"/>
            <column name="deleted_at" type="TIMESTAMP WITH TIME ZONE"/>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="course_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="enrollment_date" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

            <column name="completed" type="BOOLEAN" defaultValueBoolean="false"/>

            <column name="completion_date" type="TIMESTAMP WITH TIME ZONE"/>

            <column name="progress_percentage" type="DOUBLE PRECISION" defaultValueNumeric="0.0"/>

            <column name="user_course" type="VARCHAR(50)"/>
        </createTable>

        <!-- Restricción única user_id + course_id -->
        <addUniqueConstraint
                columnNames="user_id, course_id"
                constraintName="uk_user_course"
                tableName="user_courses"/>

        <!-- Índices -->
        <createIndex indexName="idx_user_course_user" tableName="user_courses">
            <column name="user_id"/>
        </createIndex>

        <createIndex indexName="idx_user_course_course" tableName="user_courses">
            <column name="course_id"/>
        </createIndex>

        <createIndex indexName="idx_user_course_enrollment_date" tableName="user_courses">
            <column name="enrollment_date"/>
        </createIndex>

        <!-- Foreign Keys -->
        <addForeignKeyConstraint constraintName="fk_user_course_user"
                                 baseTableName="user_courses"
                                 baseColumnNames="user_id"
                                 referencedTableName="users"
                                 referencedColumnNames="id"
                                 onDelete="CASCADE"/>

        <addForeignKeyConstraint constraintName="fk_user_course_course"
                                 baseTableName="user_courses"
                                 baseColumnNames="course_id"
                                 referencedTableName="courses"
                                 referencedColumnNames="id"
                                 onDelete="CASCADE"/>
    </changeSet>

</databaseChangeLog>
